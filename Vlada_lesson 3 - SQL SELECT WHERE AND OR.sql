-- SELECT  id,
-- 		   first_name,
--         last_name,
--         rank
-- FROM user;

--                          OR SAME THING

-- SELECT  user.id,
-- 		   user.first_name,
--         user.last_name,
--         user.rank
-- FROM user;

-- if you want specific column to be first (for comfort) -- user.* -> just because, where user is table name
-- SELECT  rank, 
--         user.*
-- FROM user;

